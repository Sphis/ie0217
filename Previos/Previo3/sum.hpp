// Esto archivo es la declaracion de una funcion

// Si no esta definido el archivo SUM_H, se define en este momento, esto es util
// para que el archivo no se genere otra vez despues de la primer ejecucion.
#ifndef SUM_H
#define SUM_H

int sum(int a, int b);

#endif